# Backend configuration
# Copy to .env and edit. Loaded automatically by dotenv at startup.

# Port the API listens on (default 5168)
PORT=5168

# Allowed CORS origin. Use a single origin in production, or omit for any.
# CORS_ORIGIN=http://localhost:3000
