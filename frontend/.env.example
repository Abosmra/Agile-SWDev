# Frontend configuration
# Copy to .env (or .env.local) and edit. Read at build time by Create React App.
# All variables exposed to the browser MUST start with REACT_APP_.

# Base URL of the backend API (default http://localhost:5168)
REACT_APP_API_BASE=http://localhost:5168
